default:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres-dbt  # Nom du service Docker
      user: dbt-user
      password: dbt-password
      port: 5432
      dbname: dbt-db
      schema: raw
      threads: 4
