name: my_dbt_project
version: "1.0.0"
profile: default  # Doit correspondre au nom du profil dans profiles.yml

model-paths: ["models"]

models:
  my_dbt_project:
    example_model:
      +materialized: table
